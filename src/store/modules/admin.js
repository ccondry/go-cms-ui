import * as types from '../mutation-types'

const state = {
  users: []
}

const getters = {
  users: state => state.users
}

const mutations = {
}

const actions = {
}

export default {
  state,
  actions,
  getters,
  mutations
}
